/**
 * Define i18n CLI configuration with type inference
 * @param {import('./config').I18nConfig} config
 * @returns {import('./config').I18nConfig}
 */
export function defineConfig(config) {
  return config;
}
